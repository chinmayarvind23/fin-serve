# ADR 002 ray serve control plane

## Decision

Use Ray Serve for distributed replica routing/placement/scaling after single-engine evidence is stable.

## Evidence

Validate the decision with the benchmark, eval, telemetry, or failure test appropriate to the component.
