# Demo Plan

Five minutes:

1. show model/deployment dashboard,
2. run baseline benchmark slice,
3. show TTFT/token throughput,
4. run optimized configuration,
5. compare latency/throughput/quality,
6. show speculative acceptance metrics,
7. show Ray replicas/GPU routing,
8. submit multimodal job,
9. trigger safe synthetic regression in staging,
10. show failed gate and rollback event.

Durable repo artifacts include architecture image, latency histogram, GPU dashboard, rollback timeline, multimodal sample, and reproduction command so the project still makes sense when the GPU deployment is offline.
