global:
  scrape_interval: 5s
  evaluation_interval: 5s
rule_files:
  - /etc/prometheus/finserve-alerts.yml
scrape_configs:
  - job_name: finserve
    static_configs:
      - targets: ["gateway:8000"]
