# ADR 011 rest grpc graphql roles

## Decision

REST/OpenAI for inference/control, selected gRPC internal boundaries, GraphQL read explorer only.

## Evidence

Validate the decision with the benchmark, eval, telemetry, or failure test appropriate to the component.
