# ADR 013 three layer autoscaling

## Decision

Tune Serve replicas, Ray worker Pods, and physical GPU nodes as separate autoscaling layers.

## Evidence

Validate the decision with the benchmark, eval, telemetry, or failure test appropriate to the component.
